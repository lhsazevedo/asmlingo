import { expect, it, vi } from "vitest";
import { render, screen } from "@testing-library/react";
import { TranslationArea } from "./";

it("renders the list of choices", () => {
  render(<TranslationArea/>);

  // Add your test here
});
